@model Nop.Web.Areas.Admin.Models.Hotels.HotelTypeModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.Hotels.HotelTypes.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("HotelTypes");
}

<form asp-controller="HotelType" asp-action="Create" method="post" id="hotelType-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Hotels.HotelTypes.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.Hotels.HotelTypes.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = AdminWidgetZones.HotelTypeDetailsButtons })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>