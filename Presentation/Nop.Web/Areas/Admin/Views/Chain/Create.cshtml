@model Nop.Web.Areas.Admin.Models.Hotels.ChainModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.Hotels.Chains.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Chains");
}

<form asp-controller="Chain" asp-action="Create" method="post" id="chain-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Hotels.Chains.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="List">@T("Admin.Hotels.Chains.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = AdminWidgetZones.ChainDetailsButtons })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>